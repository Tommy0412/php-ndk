--- a/ext/standard/dns.c
+++ b/ext/standard/dns.c
@@ -147,7 +147,8 @@
 PHP_FUNCTION(gethostname)
 {
 	char buf[256];
-	if (gethostname(buf, sizeof(buf))) {
+	/* gethostname disabled for Android - use fixed hostname */
+	if (0) {
 		RETURN_FALSE;
 	}
 	buf[sizeof(buf)-1] = '\0';
--- a/ext/random/random.c  
+++ b/ext/random/random.c
@@ -684,7 +684,8 @@
 		char buf[_POSIX_HOST_NAME_MAX];
 		struct timeval tv;
 
-		if (gethostname(buf, sizeof(buf) - 1) == 0) {
+		/* gethostname disabled for Android */
+		if (1) {
 			buf[sizeof(buf)-1] = '\0';
 			php_hash_xxh3_128(buf, strlen(buf), seed);
 		}
